import SwiftUI

struct HistoryToolbarTitle: View {
  var body: some View {
    VStack(spacing: 2) {
      Text("Trips")
        .font(FontPresets.Display.header)
      Text("ಪ್ರಯಾಣಗಳು")
        .font(FontPresets.Body.base)
    }
  }
}
